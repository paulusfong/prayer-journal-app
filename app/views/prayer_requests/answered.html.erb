<% page_title "Answered" %>
<div class="list-head">
  <h1>Answered</h1>
</div>

<% if @prayer_requests.any? %>
  <ul class="request-list">
    <%= render partial: "prayer_requests/item", collection: @prayer_requests, as: :prayer_request %>
  </ul>
<% else %>
  <p class="empty">Nothing marked answered yet.</p>
<% end %>
